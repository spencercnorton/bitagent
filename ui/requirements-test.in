-c requirements.lock

# Starlette 1.3's TestClient transport: without it `starlette.testclient` raises
# StarletteDeprecationWarning at import and the -W error suite fails.
httpx2==2.12.0
pip==26.2
pip-audit==2.10.1
pytest==9.1.1
setuptools==83.0.0
wheel==0.47.0
